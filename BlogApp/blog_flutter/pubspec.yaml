name: blog_flutter
description: A new Flutter blog app.

version: 1.0.0+1

environment:
  sdk: ">=3.3.0 <4.0.0"

dependencies:
  shared_preferences: ^2.2.0
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.6
  http: ^1.4.0
  flutter_secure_storage: ^9.2.4

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
